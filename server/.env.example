# ─────────────────────────────────────────────────────────────────────────────
# Guardian AI – Environment Variables Reference
# Copy to .env and fill in real values.
# NEVER commit .env with real secrets to version control.
# ─────────────────────────────────────────────────────────────────────────────

# ── Server ────────────────────────────────────────────────────────────────────
NODE_ENV=development
PORT=8080
LOG_LEVEL=debug                 # debug | info | warn | error
CORS_ORIGIN=http://localhost:5173

# ── PostgreSQL ────────────────────────────────────────────────────────────────
# Option A: connection string (takes precedence)
# DATABASE_URL=postgresql://guardian:guardian_secret@localhost:5432/guardian_ai

# Option B: individual fields
DB_HOST=localhost
DB_PORT=5432
DB_NAME=guardian_ai
DB_USER=guardian
DB_PASSWORD=guardian_secret
DB_SSL=false

# ── Redis ─────────────────────────────────────────────────────────────────────
REDIS_URL=redis://localhost:6379
# REDIS_URL=redis://:your_password@localhost:6379
CACHE_TTL_SECONDS=300

# ── API Keys ──────────────────────────────────────────────────────────────────

# OpenAI (required for LLM semantic analysis)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini        # gpt-4o | gpt-4o-mini | gpt-3.5-turbo

# VirusTotal v3  (https://www.virustotal.com/gui/my-apikey)
VIRUSTOTAL_API_KEY=

# Google Safe Browsing v4 (https://console.cloud.google.com)
GOOGLE_SAFE_BROWSING_API_KEY=

# IPInfo.io for IP geolocation (https://ipinfo.io/account)
IPINFO_TOKEN=

# ── API Security ──────────────────────────────────────────────────────────────
# Set this to protect all /api/* routes with a shared secret header
API_KEY=
API_KEY_HEADER=x-api-key

# ── Rate Limiting ─────────────────────────────────────────────────────────────
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100
SCAN_RATE_LIMIT_MAX=20

# ── Feature Flags ─────────────────────────────────────────────────────────────
ENABLE_ML=true
ENABLE_LLM=true
ENABLE_THREAT_INTEL=true
ENABLE_BLOCKCHAIN=false

# ── Blockchain (optional – for scan anchoring) ─────────────────────────────────
# RPC_URL=https://mainnet.infura.io/v3/YOUR_PROJECT_ID
# CHAIN_ID=1
# PRIVATE_KEY=0x...
# CONTRACT_ADDRESS=0x...
